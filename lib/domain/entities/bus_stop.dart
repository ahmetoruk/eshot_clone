class BusStop {
  final int stopId;
  final String name, stopBuses;
  final double x, y;
  BusStop(
      {required this.stopId,
      required this.name,
      required this.stopBuses,
      required this.x,
      required this.y});
}
