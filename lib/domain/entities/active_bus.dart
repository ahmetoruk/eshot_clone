class ActiveBus {
  final int busId, routeId, stopsLeft;
  final double x, y;

  ActiveBus(
      {required this.busId,
      required this.routeId,
      required this.stopsLeft,
      required this.x,
      required this.y});
}
